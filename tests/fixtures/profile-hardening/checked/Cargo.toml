[package]
name = "profile-hardening-checked"
version = "0.1.0"
edition = "2024"
publish = false

[profile.release]
overflow-checks = true
debug = true
strip = "symbols"

[profile.release-lto]
inherits = "release"
lto = "fat"
