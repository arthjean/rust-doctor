[package]
name = "suppression-lint-table"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[lints.rust]
unsafe_code = "allow"

[lints.clippy]
unwrap_used = "allow"
expect_used = { level = "allow", priority = -1 }
panic = "deny"
needless_range_loop = "allow"
