[package]
name = "feature-inventory-engine"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[features]
# Declared here and read here. The neighbouring package names it too, and does
# not declare it, which is a finding of that package and not of this one.
engine-only = []
