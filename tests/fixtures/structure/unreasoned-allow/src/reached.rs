//! Reached only through the `mod` declaration in the crate root.
//!
//! The structural pass reads the units the source kernel enumerated, so a file
//! no target names directly still carries its exemption into the report.

#[allow(unused_variables)]
pub fn reached(ignored: u8) -> u8 {
    0
}
