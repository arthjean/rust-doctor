[package]
name = "policy-gate-product-loop"
version = "0.1.0"
edition = "2024"

[dependencies]
fixture_registry_alias = { package = "fixture-registry-dep", version = "*" }
local_git_alias = { package = "local-git-dependency", git = "file://POLICY_GATE_GIT_DEPENDENCY", branch = "main" }
reqwest = { path = "../reqwest" }
