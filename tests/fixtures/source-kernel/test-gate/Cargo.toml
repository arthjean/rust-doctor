[package]
name = "source-kernel-test-gate"
version = "0.1.0"
edition = "2024"
# `benches/tests.rs` is a module of the bench target, not a bench of its own.
autobenches = false

[features]
strict = []
test-util = []

[[bench]]
name = "reach"
path = "benches/reach.rs"
harness = false
