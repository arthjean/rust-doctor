pub fn intersected(user: &str) {
    let _ = std::process::Command::new("sh")
        .arg("-c")
        .arg(format!("echo {}", user;
}

pub fn intersected_tls() {
    let _ = reqwest::Client::builder().tls_danger_accept_invalid_certs(true;
}
